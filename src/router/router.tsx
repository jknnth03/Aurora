// src/router.tsx
import { createBrowserRouter } from "react-router";
import { ROUTES } from "./routes";

// Create the router instance with your routes
export const router = createBrowserRouter(ROUTES);
